import React from "react";

const Disclaimer = () => {
	return (
		<section className="py-10">
			All amounts are in Pounds Sterling unless otherwise specified. Percentage
			returns are calculated using the simple method.
		</section>
	);
};

export default Disclaimer;
